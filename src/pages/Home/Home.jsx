import { Typography } from "@mui/material";
import React from "react";

const Home = ({ email, password }) => {
  return (
    <div>
      <Typography color="primary" variant="h2" align="center">Bienvenidos!</Typography>
    </div>
  );
};

export default Home;
